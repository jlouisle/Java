import java.util.Scanner;


public class W_3_Exercise_4 {

	public static void main(String[] args) {
		Scanner userinput = new Scanner(System.in);
		System.out.println("Enter box length (cm):");
		int length = userinput.nextInt();
		System.out.println("Enter box length (cm):");
		int width = userinput.nextInt();
		System.out.println("Enter box length (cm):");
		int height = userinput.nextInt();
		System.out.printf("The volume of your box is %d cubic centimeters.",length*width*height);
		userinput.close();
	}

}
/*
A box has dimensions length, width and height, given in centimeters.
Read the values from the command line parameters. Then calculate the
volume of the box and print it to the console.
*/